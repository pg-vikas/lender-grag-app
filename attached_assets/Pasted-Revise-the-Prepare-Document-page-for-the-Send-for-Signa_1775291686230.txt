Revise the Prepare Document page for the Send for Signature flow.

The current UI is not correct. I want this page to behave more like DocuSign in the signer assignment experience.

Make these exact changes:

1. REMOVE THE SIGNER DROPDOWN
- Remove the signer dropdown menu from the top right of the Prepare Document area.
- Do not make users switch signers through a dropdown.
- The signer should not be selected from a dropdown anywhere on this screen.

2. MOVE SIGNERS TO THE LEFT SIDEBAR
- The left sidebar should become a Signers panel.
- Every signer must appear visibly in the left sidebar at all times.
- If there are multiple signers, show them all stacked vertically in the left panel.
- Each signer should have:
  - signer name
  - signer color chip
  - role label if needed
- Under each signer, show that signer’s draggable fields directly in their section.

3. EACH SIGNER MUST HAVE THEIR OWN DRAGGABLE FIELD TOOLS
Under each signer’s section, include these drag and drop items:
- Signature
- Initial
- Date
- Checkbox
- Full Name
- Text Box

Important:
- These fields are tied directly to that signer.
- When the user drags a field from John Doe’s section, the placed field is automatically assigned to John Doe.
- When the user drags a field from Jane Smith’s section, the placed field is automatically assigned to Jane Smith.
- No extra signer selection step should be needed.

4. LEFT SIDEBAR UX
- Make the signer panel clean and premium.
- Each signer section should feel like a grouped tool block.
- Allow multiple signers with good spacing.
- Use signer color accents so field ownership is obvious.
- Add an “Add Signer” button at the top of the left sidebar.
- The signer list should be scrollable if it grows long.

5. EMPTY DOCUMENT STATE IN CENTER PANEL
In the large white document area:
- If NO document has been uploaded, do not show a blank page preview.
- Instead, show a LARGE upload dropzone that fills the main canvas area.
- This should be visually obvious and centered.

The empty state should include:
- Large upload icon
- Heading: “Upload your document”
- Subtext: “Drag and drop your file here or browse files”
- Supported formats text: PDF, DOCX, DOC, PNG, JPG
- Large primary button: “Browse Files”
- Optional secondary text/button: “Use a Template”

6. AFTER DOCUMENT UPLOAD
- Once a document is uploaded, replace the upload dropzone with the document preview canvas.
- Keep the left signer panel visible.
- Allow dragging signer-specific fields from the left into the document canvas.

7. PAGE SHOULD HAVE THIS 3-COLUMN LAYOUT
Left:
- Signers panel with signer-specific draggable fields

Center:
- Document canvas
- If no file exists, show large upload state
- If file exists, show document pages

Right:
- Field inspector for selected placed field
- Show empty state if no field is selected

8. REMOVE ANY UX THAT HIDES SIGNER CONTEXT
- No signer dropdown
- No hidden signer assignment
- Signer ownership should always be visible from the left side

9. UPDATE STATE MANAGEMENT
Refactor the drag/drop logic so that:
- draggable items in the left signer panel include recipientId
- when dropped onto the canvas, the created field automatically uses that signer’s recipientId
- no separate active signer selection is required for signer fields

10. EMPTY STATE DETAILS
If there are signers but no uploaded document:
- keep signer tools visible on left
- show large upload dropzone in center

If there are no signers and no document:
- left panel should encourage adding a signer
- center should still show large upload dropzone

11. KEEP THE DARK PREMIUM GORILLA APPS STYLE
- dark navy background
- purple accents
- premium glass cards
- smooth hover states
- clean spacing
- polished component styling

12. IMPORTANT UX GOAL
This screen should feel much more seamless and obvious:
- signer tools always visible
- no dropdown-based signer switching
- no blank white document area when no file exists
- upload action should be the main focus when the canvas is empty

Implement the actual UI and interaction updates, not just visual placeholders.