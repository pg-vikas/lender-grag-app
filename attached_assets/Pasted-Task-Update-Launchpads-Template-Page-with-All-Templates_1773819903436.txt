Task: Update **Launchpads Template Page** with All Templates filter, Template Type column, and Add New Template dropdown.

Requirements:

1. **Filter Buttons**
   * Add a button **“All Templates”** in the filter section along with existing **Task Templates** and **Document Templates** buttons.
   * Clicking **All Templates** shows **all templates combined**.
   * Clicking **Task Templates** or **Document Templates** filters templates accordingly.

2. **Template List**
   * Add a **Template Type column** to indicate whether the template is a **Task Template** or **Document Template**.
   * Ensure column is visible in all screen sizes if possible.
   * **Action column** should show **Edit** and **Delete icons** **always visible**, not just on hover.
   * **Remove Apply button** from the Action column.

3. **Add New Template Popup**
   * In the popup, the first field should be **Template Type** with a **dropdown**:
     - Options: Task Template, Document Template
   * Other fields (Template Name, Description, Category, etc.) remain as existing.
   * Popup has **Save/Submit** and **Cancel** buttons.
   * On Save, template appears in the list dynamically, with Template Type column correctly showing the type.

4. **UI & Theme**
   * All buttons, lists, popups, and icons must follow the **existing dashboard theme** (colors, spacing, typography, modal overlays).
   * Filtering, adding, editing, or deleting templates should update the list **dynamically without page reload**.

Expected Result:

* Clicking **All Templates** shows all templates, Task Templates filter shows only task templates, Document Templates filter shows only document templates.
* Template Type column displays correctly for each template.
* Action icons (Edit/Delete) are **always visible**; Apply button removed.
* Add New Template popup first field is a **dropdown for Template Type**.
* Adding/editing/deleting templates updates the list dynamically.
* Page remains responsive and visually consistent with the dashboard theme.